import '../styles/Error.css';

const Error = () => {
  return (
    <div className='fof-box'>
      <p className='fof-message'>Oh no - this page doesn't exist! Navigate to an existing page using the menu above.</p>
    </div>
  );
};

export default Error;
